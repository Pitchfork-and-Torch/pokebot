# Skill: induct

When: user accepts a CATCH.

Inputs: identify card.

Sequence:
1. Append a CARD.md to INDEX.md.
2. Remind: open the share link yourself. PokeBot never clicks Add.
3. Suggest first_training_task.
4. Slot advice: pin if Active < 6 and no duplicate, else box.

Validation: never-list is non-empty for Voice/Forge/spend-adjacent jobs.

Approval: user confirms Catch. Do not write secrets.
